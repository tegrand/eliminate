import MobileAuthOnboarding from "../components/MobileAuthOnboarding";

export default function RoleSelectionPage() {
  return <MobileAuthOnboarding initialMode="signup" initialDrawerOpen={true} />;
}
